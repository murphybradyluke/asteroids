[project]
name = "asteroids-main"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Brady Murphy", email = "murphy.bradyluke@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pygame>=2.6.1",
]

[project.scripts]
asteroids-main = "asteroids_main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
